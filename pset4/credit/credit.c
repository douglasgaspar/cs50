#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void){
    char numCartao[16];
    printf("Informe o número do cartão: ");
    scanf("%s", numCartao);

    int qtdeDigitos = strlen(numCartao);
    if(qtdeDigitos <)

    printf("%d", qtdeDigitos);

}
